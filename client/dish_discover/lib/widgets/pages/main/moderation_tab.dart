import 'package:flutter/material.dart';

import 'main.dart';

class ModerationTab extends MainPageTab {
  const ModerationTab({super.key})
      : super(
            title: 'Moderation', body: const Center(child: Text('Moderation')));
}
