import 'package:flutter/material.dart';

import 'main.dart';

class RecommendedTab extends MainPageTab {
  const RecommendedTab({super.key})
      : super(
            title: 'Recommended',
            body: const Center(child: Text('Recommended')));
}
