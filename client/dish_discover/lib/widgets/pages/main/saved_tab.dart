import 'package:flutter/material.dart';

import 'main.dart';

class SavedTab extends MainPageTab {
  const SavedTab({super.key})
      : super(title: 'Saved', body: const Center(child: Text('Saved')));
}
